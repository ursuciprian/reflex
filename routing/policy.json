{
  "name": "Model routing",
  "version": "routing-v1",
  "subject": "Which model serves a request, by how sensitive its content is and how hard it is. Routing stays inside one family: the clients' credentials are not interchangeable across providers.",
  "latency_budget_ms": 1500,
  "cache_ttl_s": 3600,
  "fallback": {
    "sensitivity": "restricted",
    "difficulty": 1,
    "note": "Any Jev error or timeout: treat the content as restricted and the task as medium."
  },
  "sensitivity": {
    "order": ["public", "application", "restricted", "proprietary"],
    "restricted_at": 0.25,
    "restricted_at_note": "Treat the request as restricted when P(restricted) + P(proprietary) reaches this, whatever the top choice was.",
    "restricted_paths": [
      "(^|/)\\.env(\\.[\\w-]+)?$",
      "\\.(pem|key|p12|pfx|tfvars|tfstate)$",
      "(^|/)(id_rsa|id_ed25519|\\.netrc|\\.npmrc|\\.pypirc)$",
      "(^|/)\\.aws/(credentials|config)$",
      "(^|/)\\.kube/config$",
      "(^|/)\\.ssh/",
      "secret",
      "(^|/)envs?/prod"
    ],
    "pools": {
      "public": {"require": [], "note": "Any model in the family; cheapest first."},
      "application": {"require": ["first_party"], "note": "No third-party hosts."},
      "restricted": {"require": ["first_party", "frontier"], "note": "First-party frontier only."},
      "proprietary": {"require": ["first_party", "frontier"], "note": "First-party frontier only."}
    }
  },
  "tiers": {
    "order": ["small", "medium", "large"],
    "from_score": {"small": 0, "medium": 0.6, "large": 1.4},
    "note": "difficulty is a 0-2 score; the tier is the highest whose threshold the score reaches. A model of a higher tier may serve a lower one, never the reverse."
  },
  "needs_tools_at": 0.5,
  "stickiness": {
    "cache_read_factor": 0.1,
    "turn_tokens": 3000,
    "remaining_turns": 1,
    "note": "Mid-conversation, a cheaper model is only taken when (context x cache_read_factor + turn_tokens) x (old - new input price) x remaining_turns exceeds context x new input price, the cost of the new model reprocessing the whole context. remaining_turns 1: the switch must pay for itself on the next turn, because nobody knows how many turns are left; at 1/cache_read_factor turns the context term cancels and every cheaper model wins. Moves to a higher tier, and moves forced by sensitivity, always happen."
  },
  "families": {
    "claude": {
      "match": ["claude-*"],
      "note": "Claude Code via the forwarded subscription login. Prices are list USD per million input tokens; edit to your contract.",
      "models": {
        "claude-haiku-4-5": {"tier": "small", "price_in": 1.0, "first_party": true, "frontier": false},
        "claude-sonnet-5": {"tier": "medium", "price_in": 3.0, "first_party": true, "frontier": true},
        "claude-opus-5": {"tier": "large", "price_in": 5.0, "first_party": true, "frontier": true}
      }
    },
    "chatgpt": {
      "match": ["gpt-*", "auto"],
      "note": "Codex via the ChatGPT subscription. Prices are placeholders for relative cost; edit to your contract.",
      "models": {
        "gpt-5.6-luna": {"tier": "small", "price_in": 0.25, "first_party": true, "frontier": false},
        "gpt-5.6-terra": {"tier": "medium", "price_in": 1.25, "first_party": true, "frontier": true},
        "gpt-5.6-sol": {"tier": "large", "price_in": 5.0, "first_party": true, "frontier": true}
      }
    }
  }
}
