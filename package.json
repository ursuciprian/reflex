{
  "name": "reflex",
  "version": "0.1.0",
  "description": "Risk gate for coding-agent shell commands: deterministic rules + TypeSafe Jev judgments + an editable policy",
  "private": true,
  "type": "module",
  "engines": {"node": ">=18"},
  "scripts": {
    "test": "node policy.mjs && node gate.mjs --selfcheck && node instructions.mjs --selfcheck && node router/server.mjs --selfcheck",
    "eval": "node eval.mjs",
    "eval-instructions": "node eval-instructions.mjs",
    "eval-router": "node router/server.mjs --eval",
    "report": "node report.mjs",
    "install-hook": "node install.mjs"
  }
}
