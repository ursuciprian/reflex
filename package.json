{
  "name": "reflex",
  "version": "0.1.0",
  "description": "Risk gate for coding-agent shell commands: deterministic rules + TypeSafe Jev judgments + an editable policy",
  "private": true,
  "type": "module",
  "engines": {"node": ">=18"},
  "scripts": {
    "test": "node policy.mjs && node gate.mjs --selfcheck && python3 routing/reflex_router.py --selfcheck",
    "eval": "node eval.mjs",
    "report": "node report.mjs",
    "install-hook": "node install.mjs"
  }
}
